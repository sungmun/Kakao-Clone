import { SearchBar } from 'components/Base/SearchBar';
import * as React from 'react';

const searchBarContainer: React.SFC = () => <SearchBar />;

export { searchBarContainer as SearchBarContainer };
