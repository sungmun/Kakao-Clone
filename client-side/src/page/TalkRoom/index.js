import React from 'react';
import Layout from 'layout/List';
import TalkRoom from 'component/TalkRoom';

export default () => {
    return (
        <Layout>
            <TalkRoom />
        </Layout>
    );
};
